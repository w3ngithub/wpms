import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1f80d4",
      mainGradient: "linear-gradient(#337ab7, #5490c3)",
    },
    secondary: {
      main: "#1f80d4",
    },
  },
});
export default theme;
